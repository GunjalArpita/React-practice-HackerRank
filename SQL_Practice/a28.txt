/*
Enter your query below.
Please append a semicolon ";" at the end of the query
*/
SELECT 
c.customer_id,c.name,CONCAT('+',cc.country_code,c.phone_number)
AS phone_number
FROM customers c
join country_codes cc 
ON c.country = cc.country 
ORDER BY
c.customer_id ASC;
